#include "quantum.h"

enum custom_keycodes {
    VIM_TOGGLE = QK_KB_0,
    RM_RESET,
    PC_LOCK,
    SENS_SHIFT,
};

enum layers {
    _QWERTY = 0,
    _SYM,
    _NAV,
    _NAV_UP,
    _FKEYS,
    _HUB,
    _MOUSE_KEYS,
};
